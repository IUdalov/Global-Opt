# Global-Opt #
#### Building UNIX Only####
 - `cd <path>/Global-Opt`
 - `mkdir build; cd build` - Create build filder.
 - `cmake ../cinterface` - Run CMake.
 - `make` - Done!

#### Планы на будущее ####
 - добавить поддержку windows (.dll)
